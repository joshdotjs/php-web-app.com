{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "deploy": "npm run build  &&  git add .  &&  git commit -m 'deploy'  &&  git push prod main  &&  ssh root@php-web-app.com;  cd /var/www/ourapp"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.13",
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.4.21",
        "tailwindcss": "^3.2.4",
        "vite": "^4.0.0"
    },
    "engines": {
        "npm": ">=8.0.0 <9.0.0",
        "node": ">=16.0.0 <17.0.0"
    }
}
